apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: container-registry-write-credentials
  namespace: kafka-connect
spec:
  dataFrom:
  - extract:
      key: container-registry-write-credentials
  secretStoreRef:
    kind: ClusterSecretStore
    name: local-kubernetes-secret-store
  target:
    creationPolicy: Owner
    name: container-registry-write-credentials
    template:
      data:
        .dockerconfigjson: '{"auths":{"rg.nl-ams.scw.cloud":{"auth":"{{ printf "%s:%s"
          .SCALEWAY_ACCESS_KEY .SCALEWAY_SECRET_KEY | b64enc }}"}}}'
      type: kubernetes.io/dockerconfigjson
