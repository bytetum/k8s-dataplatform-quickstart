apiVersion: v1
kind: Service
metadata:
  name: postgres-m3-test
  namespace: lakehouse-kafka-connect
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: postgres-m3-test
  type: ClusterIP
